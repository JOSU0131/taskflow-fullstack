# URL del backend (sin barra final).
# - En desarrollo local apunta al servidor Express:
#     VITE_API_URL=http://localhost:4000
# - En producción (Vercel monorepo) déjalo vacío para usar el mismo origen
#   y aprovechar las rewrites de vercel.json:
#     VITE_API_URL=
VITE_API_URL=http://localhost:4000
